[project]
name = "orchflow"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.6.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "pyyaml>=6.0.3",
]

[project.optional-dependencies]
dev = [
    "black>=26.5.1",
]

[tool.uv]
package = false
